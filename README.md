A simple script to scrobble downloaded Spotify history to last.fm. At least `artistName` and `albumName` fields are required. 
Lastfm username, password, and API Keys also need to be setup in a `.env` file at the root.
